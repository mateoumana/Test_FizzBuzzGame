Para compilarlo se debe hacer uso de Cmake y un compilador de C/C++ que soporte hilos.
Se ubica en la carpeta build, se ejecuta el comando "cmake .." y luego "cmake --build ." en windows, en linux "make"
